# Ansible deploy config on Docker Image

## Issue

Makefile: ${DOCKER_MACHINE_IP} is static 

## HOW TO

### Make ssh connection

cmd: `make ssh_link`

### Remote ssh

cmd: `make ssh_connect`

### RUN Docker Image

cmd: `make ubu`


